@model IEnumerable<WebApplication4.Models.DataEvents>

<br />
<br />
<p>
    @Html.ActionLink("Create New", "AddEvents")
</p>

<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.EventDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Detail)
        </th>
        <th></th>
    </tr>

    @*@foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Holiday)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details)

            </td>
            <td>
                @Html.ActionLink("Edit", "UpdateHoliday", new { id = item.Details })

                @Html.ActionLink("Delete", "DeleteHoliday", new { id = item.Details })
            </td>
        </tr>
    }*@

</table>



