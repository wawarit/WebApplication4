
<!--@{
    ViewBag.Title = "Testview";
}
<style media="screen">
    .container {
        border: 0px solid darkgrey;
        border-radius: 3px;
        padding: 5px;
        width: 99%;
        margin: 0 auto;
    }
</style>

<!DOCTYPE html>
<html>
<head>
    <title>ThaiCreate.Com</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <script src="http://code.jquery.com/jquery-latest.js"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            // retrieve data from the database and add it to the table
    // retrieve data from the server-side code and add it to the table
            $.ajax({
                url: '@Url.Action("GetData", "Calendar")',
                type: "GET",
                /*url: "/Home/GetData",*/ // replace with the URL of your server-side code that retrieves the data from the database
                success: function (data) {
                // iterate over the array of objects and create a row for each one
                    $.each(data, function (index, item) {
                        var tr = "<tr>";
                        tr = tr + "<td>" + item.Day_Holiday + "</td>";
                        tr = tr + "<td>" + item.Details + "</td>";
                }
            });
            $("#deleteRows").click(function () {
                if ($("#myTable tr").length != 1) {
                    $("#myTable tr:last").remove();
                }
            });
            // code for deleteRows and clearRows buttons is the same as before
        });
            $("#clearRows").click(function () {
                $('#myTable > tbody:last').empty(); // remove all
            });
    </script>
    <meta charset=utf-8 />
</head>
<body>
    <center>
        <h1>Calendar</h1>
        <table width="600" border="1" id="myTable">-->
            <!-- head table -->
            <!--<thead>
                <tr>
                    <td width="91"> <div align="center">Day_Holiday </div></td>
                    <td width="98"> <div align="center">Details </div></td>
            </thead>-->
            <!-- body dynamic rows -->
            <!--<tbody></tbody>
        </table>
        <br />
        <input type="button" id="deleteRows" value="Delete Last Row">
        <input type="button" id="clearRows" value="Clear Table">
        <center>
</body>
</html>-->
